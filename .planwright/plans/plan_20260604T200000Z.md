# planwright Plan — .
<!-- Session: 2026-06-04T22:00:00Z -->

- [x] Implement getSystemLoadAverage (declared but missing)
      Mode: develop
      Rationale: getSystemLoadAverage is declared in the public API and the README advertises load-average monitoring, but no implementation exists; the model and /proc/loadavg seam both exist.
      Evidence: include/analyzer/core.h:57 declares `utils::Result<SystemLoadAverage> getSystemLoadAverage() const;` but no `ProcessAnalyzer::getSystemLoadAverage` definition exists in src/ (grep finds zero; nm confirms absent from libprocessAnalyzerLib.a); SystemLoadAverage (include/analyzer/system_model.h:24) has oneMin/fiveMin/fifteenMin.
      Surfaces: src/analyzer/system.cpp
      New Surfaces: tests/analyzer/system_load_average.cpp
      Development: Implement ProcessAnalyzer::getSystemLoadAverage in src/analyzer/system.cpp after getSystemMemoryInfo: read (procPath / "loadavg") with utils::readTextFile and propagate its error, then use std::istringstream to parse three space-separated doubles into SystemLoadAverage.oneMin, fiveMin, fifteenMin; propagate parse error on failure. Create tests/analyzer/system_load_average.cpp with GetSystemLoadAverageTest fixture (same MockProc pattern as GetSystemMemoryInfoTest): mockProc->createFileAt("loadavg", "0.52 1.23 0.87 1/450 12345\n"), call getSystemLoadAverage(), assert oneMin≈0.52, fiveMin≈1.23, fifteenMin≈0.87; add a second test where the loadavg file is absent and assert the result holds an error.
      Acceptance: getSystemLoadAverage parses /proc/loadavg into SystemLoadAverage; new GetSystemLoadAverageTest passes and all existing tests still pass.
      Verification: ctest --test-dir build -R "GetSystemLoadAverageTest" --output-on-failure

- [x] Implement getSystemActivityStats (declared but missing)
      Mode: develop
      Rationale: getSystemActivityStats is declared in the public API with no implementation; MockProc::createSystemStat already writes the ctxt and processes fields needed for testing.
      Evidence: include/analyzer/core.h:61 declares `utils::Result<SystemActivityStats> getSystemActivityStats() const;` but no `ProcessAnalyzer::getSystemActivityStats` definition exists in src/; SystemActivityStats (include/analyzer/system_model.h:83) has interruptsTotal, interruptsPerCpu, contextSwitches, processesForked; MockProc::SystemStatData (tests/utils/testing_framework.h) has ctxt and processes fields; MockProc::createSystemStat exists (tests/utils/testing_framework.cpp:341-ish) to write /proc/stat.
      Surfaces: src/analyzer/system.cpp
      New Surfaces: tests/analyzer/system_activity_stats.cpp
      Development: Implement ProcessAnalyzer::getSystemActivityStats in src/analyzer/system.cpp: read (procPath / "stat") with utils::readTextFile and propagate its error. For each line: if it starts with "intr " parse the first number as interruptsTotal and subsequent space-separated numbers as interruptsPerCpu["cpu0"], ["cpu1"], …; if "ctxt " parse contextSwitches; if "processes " parse processesForked. Create tests/analyzer/system_activity_stats.cpp with GetSystemActivityStatsTest: use mockProc->createSystemStat with ctxt=54321 and processes=678, call getSystemActivityStats(), assert contextSwitches==54321 and processesForked==678; also test missing /proc/stat file returns error.
      Acceptance: getSystemActivityStats parses /proc/stat into SystemActivityStats; new GetSystemActivityStatsTest passes and all existing tests still pass.
      Verification: ctest --test-dir build -R "GetSystemActivityStatsTest" --output-on-failure

- [x] Implement getSystemDiskIoStats (declared but missing)
      Mode: develop
      Rationale: getSystemDiskIoStats is declared in the public API with no implementation; DiskIoDeviceStats is fully defined with all 11 I/O counter fields and /proc/diskstats format is well-known.
      Evidence: include/analyzer/core.h:59 declares `utils::Result<std::vector<DiskIoDeviceStats>> getSystemDiskIoStats() const;` with no definition in src/; DiskIoDeviceStats (include/analyzer/system_model.h:54) has deviceName, readsCompleted, readsMerged, sectorsRead, readTimeMs, writesCompleted, writesMerged, sectorsWritten, writeTimeMs, ioProgressMs, ioWeightedTimeMs.
      Surfaces: src/analyzer/system.cpp
      New Surfaces: tests/analyzer/system_disk_io_stats.cpp
      Development: Implement ProcessAnalyzer::getSystemDiskIoStats in src/analyzer/system.cpp: read (procPath / "diskstats") with utils::readTextFile and propagate its error. For each line use std::istringstream to read major, minor, deviceName, then the 11 I/O counters in order into a DiskIoDeviceStats; push each entry into a vector and return it (return error if the file is empty or unparseable). Create tests/analyzer/system_disk_io_stats.cpp with GetSystemDiskIoStatsTest: use mockProc->createFileAt("diskstats", " 8   0 sda 1000 200 50000 1200 500 100 20000 600 0 800 1800 0 0 0 0 0 0\n"), call getSystemDiskIoStats(), assert result has one entry with deviceName=="sda", readsCompleted==1000, writesCompleted==500; test missing diskstats returns error.
      Acceptance: getSystemDiskIoStats parses /proc/diskstats into a vector of DiskIoDeviceStats; new GetSystemDiskIoStatsTest passes and all existing tests still pass.
      Verification: ctest --test-dir build -R "GetSystemDiskIoStatsTest" --output-on-failure

- [x] Implement getProcessResourceLimits (declared but missing)
      Mode: develop
      Rationale: getProcessResourceLimits is declared in the public API with no implementation; ResourceLimitInfo and ResourceLimit structs are defined in process_model.h and /proc/<pid>/limits has a well-known tabular format.
      Evidence: include/analyzer/core.h:47 declares `utils::Result<ResourceLimitInfo> getProcessResourceLimits(int pid) const;` with no definition in src/ (grep finds zero); ResourceLimitInfo (include/analyzer/process_model.h) wraps a vector<ResourceLimit>{resource, softLimit, hardLimit, units}.
      Surfaces: src/analyzer/details.cpp
      New Surfaces: tests/analyzer/resource_limits.cpp
      Development: Implement ProcessAnalyzer::getProcessResourceLimits in src/analyzer/details.cpp after getProcessMemoryMaps: call Internal::checkPidPathExistsAndPermissions(procPath, pid) and propagate its error; read (procPath / std::to_string(pid) / "limits") with utils::readTextFile; skip the header line; for each subsequent line parse resource name (columns 1..N-3), soft limit, hard limit, units using a fixed-column or token-based parse; push each ResourceLimit into ResourceLimitInfo.limits. Create tests/analyzer/resource_limits.cpp with GetProcessResourceLimitsTest: use mockProc->createFileAt(std::to_string(pid)+"/limits", header+"\nMax cpu time         unlimited  unlimited  seconds\nMax file size        1048576    unlimited  bytes\n"), call getProcessResourceLimits(pid), assert two entries with correct resource/soft/hard/units; assert absent pid returns error.
      Acceptance: getProcessResourceLimits parses /proc/<pid>/limits into ResourceLimitInfo; new GetProcessResourceLimitsTest passes and all existing tests still pass.
      Verification: ctest --test-dir build -R "GetProcessResourceLimitsTest" --output-on-failure

- [x] Implement getProcessCgroupInfo (declared but missing)
      Mode: develop
      Rationale: getProcessCgroupInfo is declared in the public API with no implementation; CgroupInfo/CgroupEntry structs are defined in process_model.h and /proc/<pid>/cgroup has a simple colon-delimited line format.
      Evidence: include/analyzer/core.h:48 declares `utils::Result<CgroupInfo> getProcessCgroupInfo(int pid) const;` with no definition in src/; CgroupEntry (include/analyzer/process_model.h) has id, controllers, path; CgroupInfo wraps a vector<CgroupEntry>.
      Surfaces: src/analyzer/details.cpp
      New Surfaces: tests/analyzer/cgroup_info.cpp
      Development: Implement ProcessAnalyzer::getProcessCgroupInfo in src/analyzer/details.cpp after getProcessResourceLimits: call Internal::checkPidPathExistsAndPermissions(procPath, pid) and propagate; read (procPath / std::to_string(pid) / "cgroup") with utils::readTextFile; for each non-empty line split on ':' into three tokens — id (integer), controllers (may be empty in v2), path — and push CgroupEntry. Create tests/analyzer/cgroup_info.cpp with GetProcessCgroupInfoTest: createFileAt(pid+"/cgroup", "12:cpu,cpuacct:/docker/abc\n0::/\n"), call getProcessCgroupInfo(pid), assert two entries with correct id/controllers/path values; assert absent pid returns error.
      Acceptance: getProcessCgroupInfo parses /proc/<pid>/cgroup into CgroupInfo; new GetProcessCgroupInfoTest passes and all existing tests still pass.
      Verification: ctest --test-dir build -R "GetProcessCgroupInfoTest" --output-on-failure

- [x] Implement getSystemDiskUsage (declared but missing)
      Mode: develop
      Rationale: getSystemDiskUsage is declared in the public API with no implementation; MountPointInfo (mountPoint, filesystemType, device, totalSpaceBytes, freeSpaceBytes, availableSpaceBytes) is defined and statvfs() provides the space fields.
      Evidence: include/analyzer/core.h:58 declares `utils::Result<std::vector<MountPointInfo>> getSystemDiskUsage() const;` with no definition in src/; MountPointInfo (include/analyzer/system_model.h:98) has the six fields; no ProcessAnalyzer::getSystemDiskUsage symbol appears in nm output.
      Surfaces: src/analyzer/system.cpp
      New Surfaces: tests/analyzer/system_disk_usage.cpp
      Development: Implement ProcessAnalyzer::getSystemDiskUsage in src/analyzer/system.cpp: read (procPath / "mounts") with utils::readTextFile and propagate its error. For each line parse device, mountPoint, filesystemType; skip entries whose filesystemType is in {"proc","sysfs","devtmpfs","cgroup","cgroup2","tmpfs","devpts","hugetlbfs","mqueue","debugfs","tracefs","securityfs","pstore"} to avoid pseudo-filesystems; call statvfs(mountPoint.c_str(), &sv) and on success populate totalSpaceBytes = sv.f_blocks * sv.f_frsize, freeSpaceBytes = sv.f_bfree * sv.f_frsize, availableSpaceBytes = sv.f_bavail * sv.f_frsize; add #include <sys/statvfs.h>. Create tests/analyzer/system_disk_usage.cpp with GetSystemDiskUsageTest: create "mounts" content pointing to an existing real path (mockProc->getPath()) for the mount point with type "ext4"; call getSystemDiskUsage(), assert at least one entry with correct device/mountPoint/filesystemType and space values > 0; test missing mounts file returns error.
      Acceptance: getSystemDiskUsage parses /proc/mounts and calls statvfs per real mount into MountPointInfo; new GetSystemDiskUsageTest passes and all existing tests still pass.
      Verification: ctest --test-dir build -R "GetSystemDiskUsageTest" --output-on-failure

- [x] Implement getSystemInfo (declared but missing)
      Mode: develop
      Rationale: getSystemInfo is declared in the public API with no implementation; SystemInfo{uptime, kernelVersion, osName, hostname} aggregates data from /proc/uptime, /proc/version, and the hostname seam; MockProc already provides createUptime and createVersion helpers.
      Evidence: include/analyzer/core.h:53 declares `utils::Result<SystemInfo> getSystemInfo() const;` with no definition in src/; SystemInfo (include/analyzer/system_model.h:108) has uptime (chrono::seconds), kernelVersion (string), osName (string), hostname (string); MockProc::createUptime (tests/utils/testing_framework.h) and MockProc::createVersion exist.
      Surfaces: src/analyzer/system.cpp
      New Surfaces: tests/analyzer/system_info.cpp
      Development: Implement ProcessAnalyzer::getSystemInfo in src/analyzer/system.cpp: read (procPath / "uptime") with utils::readTextFile, parse first double as uptime seconds → std::chrono::seconds(static_cast<long>(val)); read (procPath / "version") → kernelVersion (full first line); read (procPath / "sys" / "kernel" / "hostname") via utils::readTextFile → hostname (trim whitespace); for osName try reading (procPath / "etc" / "os-release") and extract the NAME= value (strip quotes), fallback to "Linux". Propagate error from uptime or version read; hostname/osName failures are non-fatal (use empty string). Create tests/analyzer/system_info.cpp with GetSystemInfoTest: use mockProc->createUptime(7200, 100), mockProc->createVersion("Linux version 6.1.0-test"), createFileAt("sys/kernel/hostname", "testhost\n"), call getSystemInfo(), assert uptime==7200s, kernelVersion contains "6.1.0", hostname=="testhost"; test absent uptime file returns error.
      Acceptance: getSystemInfo returns SystemInfo populated from /proc/uptime, /proc/version, and hostname; new GetSystemInfoTest passes and all existing tests still pass.
      Verification: ctest --test-dir build -R "GetSystemInfoTest" --output-on-failure

- [x] Make streamQueryProcesses truly lazy for filter-only queries
      Mode: improve
      Rationale: streamQueryProcesses at src/analyzer/base.cpp:207 calls queryProcesses() internally, materializing all matching processes into a vector before any co_yield fires; the comment "it won't be truly streaming" confirms this is known technical debt that defeats the lazy-generator contract.
      Evidence: src/analyzer/base.cpp:207 `auto queryResult = queryProcesses(filter, sortBy, sortOrder);` inside the coroutine body; the comment at base.cpp:196-205 explicitly says "Given the task is just to get it to compile, let's just make it yield from queryProcesses (which means it won't be truly streaming, but will work)."
      Surfaces: src/analyzer/base.cpp
      New Surfaces: tests/analyzer/stream_query.cpp
      Development: In src/analyzer/base.cpp streamQueryProcesses: replace the queryProcesses() call with a direct coroutine body that iterates over streamPids(), calls getProcessDetails(pid) per pid (same as streamProcesses()), applies the ProcessFilter inline using the same applyStringFilter / applyRangeFilter helpers from query.cpp (move them to a shared header or duplicate the necessary checks), and co_yields matching processes. When sortBy is not ProcessSortField::pid or sortOrder is not SortOrder::asc (i.e., a sort is explicitly requested), fall back to calling queryProcesses() and yielding from the result — sorting inherently requires buffering all results. Create tests/analyzer/stream_query.cpp with StreamQueryProcessesTest fixture (MockProc pattern): build three processes with distinct names, call streamQueryProcesses with nameContains filter matching only one, collect results into a vector by draining the generator, assert only the matching process is returned; verify default sort (pid/asc) also works.
      Acceptance: streamQueryProcesses with filter-only query streams without materializing all results; sorted queries still work correctly; new StreamQueryProcessesTest passes and all existing tests still pass.
      Verification: ctest --test-dir build -R "StreamQueryProcessesTest" --output-on-failure
